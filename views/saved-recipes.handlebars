<section class="container">
  <h3 class="sub-header">My Recipes</h3>
  <section class="flex-row justify-center">
    {{#each recipes as |recipe|}}
    {{> recipe-details}}
    {{/each}}
  </section>
</section>